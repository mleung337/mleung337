# Matthew Leung

# Asks for a word and number then checks if number larger than number of letters in word using len
word = input('Gimme a word. ')
number = int(input('Gimme a number larger than the number of letters in that word. '))
print(number > len(word))
    
